package resources;

public class filesController {
    private filesManager model;
    private mainMenu view;

    public  filesController(filesManager model, mainMenu view){
        this.model = model;
    }
}
